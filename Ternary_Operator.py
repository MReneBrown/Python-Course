role = 'admin'

auth = 'can access' if role == 'admin' else 'cannot access'

"""
traditional syntax of conditional
if role == 'admin'
    auth = 'can access'
else:
    auth = 'cannot access'
"""

print(auth)

cann access

